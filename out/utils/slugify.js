// Generated by IcedCoffeeScript 1.3.3g
(function() {
  var slug, slugify, slugs, urlify;

  urlify = require('urlify').create({
    addEToUmlauts: false,
    szToSs: true,
    spaces: '-',
    nonPrintable: '-',
    trim: true
  });

  slug = require('slug');

  slugs = {};

  slugify = function(str) {
    var res;
    res = slugs[str];
    if (res == null) {
      res = urlify(slug(str)).toLowerCase();
      slugs[str] = res;
    }
    return res;
  };

  module.exports = slugify;

}).call(this);
